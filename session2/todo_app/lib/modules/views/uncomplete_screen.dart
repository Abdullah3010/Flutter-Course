import 'package:flutter/material.dart';

class UncompleteTasksScreen extends StatelessWidget {
  const UncompleteTasksScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('uncomplete Tasks'),
    );
  }
}
